# Generated from src/modules/shared/backend/constants/domain-catalog.json
# Do not hand-edit pack/upstream fields. Run: npm run domain:manifests
domain: system
owner: system
kind: platform
stage: A
contractVersion: v1
implementation: local-ts
publicPrefix:
  - /api/v1/admin/system
upstream:
  env: RUOYI_DOMAIN_SYSTEM_UPSTREAM
  protocol: http
  defaultPort: 3210
auth:
  audience: admin
  tenantPolicy: required
resilience:
  timeoutMs: 5000
  retry:
    maxAttempts: 1
    safeMethodsOnly: true
  idempotencyRequired: false
rollout:
  mode: local
messaging:
  transport: in-process
  queueGroup: ruoyi.system
  commandSubject: ruoyi.cmd.system.>
  eventSubject: ruoyi.evt.system.>
invoke:
  colocated: sdk
  remote: rpc
  protocol: nats-rr
  serialization: json
  facade: required
pack:
  enabled: true
  kind: api-only
  independentDatabase: false
  dependsOnModules:
    - shared
    - system
