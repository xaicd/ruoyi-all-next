# Generated from src/modules/shared/backend/constants/domain-catalog.json
# Do not hand-edit pack/upstream fields. Run: npm run domain:manifests
domain: iot
owner: iot
kind: business
stage: B
contractVersion: v1
implementation: local-ts
publicPrefix:
  - /api/v1/admin/iot
upstream:
  env: RUOYI_DOMAIN_IOT_UPSTREAM
  protocol: http
  defaultPort: 3224
auth:
  audience: admin
  tenantPolicy: required
resilience:
  timeoutMs: 5000
  retry:
    maxAttempts: 1
    safeMethodsOnly: true
  idempotencyRequired: true
rollout:
  mode: local
messaging:
  transport: in-process
  queueGroup: ruoyi.iot
  commandSubject: ruoyi.cmd.iot.>
  eventSubject: ruoyi.evt.iot.>
invoke:
  colocated: sdk
  remote: rpc
  protocol: nats-rr
  serialization: json
  facade: required
pack:
  enabled: true
  kind: api-only
  independentDatabase: false
  dependsOnModules:
    - shared
    - iot
