# Generated from src/modules/shared/backend/constants/domain-catalog.json
# Do not hand-edit pack/upstream fields. Run: npm run domain:manifests
domain: member
owner: member
kind: business
stage: A
contractVersion: v1
implementation: local-ts
publicPrefix:
  - /api/v1/admin/member
  - /api/v1/app/member
upstream:
  env: RUOYI_DOMAIN_MEMBER_UPSTREAM
  protocol: http
  defaultPort: 3218
auth:
  audience: admin
  tenantPolicy: required
resilience:
  timeoutMs: 5000
  retry:
    maxAttempts: 1
    safeMethodsOnly: true
  idempotencyRequired: false
rollout:
  mode: local
messaging:
  transport: in-process
  queueGroup: ruoyi.member
  commandSubject: ruoyi.cmd.member.>
  eventSubject: ruoyi.evt.member.>
invoke:
  colocated: sdk
  remote: rpc
  protocol: nats-rr
  serialization: json
  facade: required
pack:
  enabled: true
  kind: api-only
  independentDatabase: false
  dependsOnModules:
    - shared
    - member
