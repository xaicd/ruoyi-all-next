# Generated from src/modules/shared/backend/constants/domain-catalog.json
# Do not hand-edit pack/upstream fields. Run: npm run domain:manifests
domain: report
owner: report
kind: business
stage: A
contractVersion: v1
implementation: local-ts
publicPrefix:
  - /api/v1/admin/report
upstream:
  env: RUOYI_DOMAIN_REPORT_UPSTREAM
  protocol: http
  defaultPort: 3215
auth:
  audience: admin
  tenantPolicy: required
resilience:
  timeoutMs: 15000
  retry:
    maxAttempts: 1
    safeMethodsOnly: true
  idempotencyRequired: false
rollout:
  mode: local
messaging:
  transport: in-process
  queueGroup: ruoyi.report
  commandSubject: ruoyi.cmd.report.>
  eventSubject: ruoyi.evt.report.>
invoke:
  colocated: sdk
  remote: rpc
  protocol: nats-rr
  serialization: json
  facade: required
pack:
  enabled: true
  kind: api-only
  independentDatabase: false
  dependsOnModules:
    - shared
    - report
