// Auto-generated by Codegen Engine
import { z } from "zod"



export const wmsWarehousePageQuerySchema = z.object({
  page: z.coerce.number().int().min(1).default(1),
  pageSize: z.coerce.number().int().min(1).max(100).default(20),
  code: z.string().trim().max(200).optional(),
  name: z.string().trim().max(200).optional(),
}).strict()

export const wmsWarehouseCreateSchema = z.object({
  code: z.string().trim().min(1, "仓库编码 不能为空"),
  name: z.string().trim().min(1, "仓库名称 不能为空"),
  capacity: z.coerce.number().finite().optional(),
  active: z.preprocess((value) => value === "true" ? true : value === "false" ? false : value, z.boolean()),

}).strict()

export const wmsWarehouseUpdateSchema = wmsWarehouseCreateSchema.partial().extend({
  id: z.string().min(1, "id 不能为空"),
}).strict()

export type WmsWarehousePageQueryInput = z.infer<typeof wmsWarehousePageQuerySchema>
export type WmsWarehouseCreateInput = z.infer<typeof wmsWarehouseCreateSchema>
export type WmsWarehouseUpdateInput = z.infer<typeof wmsWarehouseUpdateSchema>
