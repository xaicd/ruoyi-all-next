// Auto-generated by Codegen Engine
import { z } from "zod"



export const wmsItemSkuPageQuerySchema = z.object({
  page: z.coerce.number().int().min(1).default(1),
  pageSize: z.coerce.number().int().min(1).max(100).default(20),
  item_id: z.string().trim().max(200).optional(),
  sku_code: z.string().trim().max(200).optional(),
  sku_name: z.string().trim().max(200).optional(),
}).strict()

export const wmsItemSkuCreateSchema = z.object({
  item_id: z.string().trim().min(1, "物料ID 不能为空"),
  sku_code: z.string().trim().min(1, "SKU编码 不能为空"),
  sku_name: z.string().trim().min(1, "规格名称 不能为空"),
  barcode: z.string().trim().optional(),
  price: z.coerce.number().finite().optional(),

}).strict()

export const wmsItemSkuUpdateSchema = wmsItemSkuCreateSchema.partial().extend({
  id: z.string().min(1, "id 不能为空"),
}).strict()

export type WmsItemSkuPageQueryInput = z.infer<typeof wmsItemSkuPageQuerySchema>
export type WmsItemSkuCreateInput = z.infer<typeof wmsItemSkuCreateSchema>
export type WmsItemSkuUpdateInput = z.infer<typeof wmsItemSkuUpdateSchema>
