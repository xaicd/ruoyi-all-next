// Auto-generated by Codegen Engine
import { z } from "zod"



export const wmsMerchantPageQuerySchema = z.object({
  page: z.coerce.number().int().min(1).default(1),
  pageSize: z.coerce.number().int().min(1).max(100).default(20),
  code: z.string().trim().max(200).optional(),
  name: z.string().trim().max(200).optional(),
}).strict()

export const wmsMerchantCreateSchema = z.object({
  code: z.string().trim().min(1, "货主编码 不能为空"),
  name: z.string().trim().min(1, "货主名称 不能为空"),
  contact_name: z.string().trim().optional(),
  contact_phone: z.string().trim().optional(),
  status: z.string().trim().min(1, "状态 不能为空"),

}).strict()

export const wmsMerchantUpdateSchema = wmsMerchantCreateSchema.partial().extend({
  id: z.string().min(1, "id 不能为空"),
}).strict()

export type WmsMerchantPageQueryInput = z.infer<typeof wmsMerchantPageQuerySchema>
export type WmsMerchantCreateInput = z.infer<typeof wmsMerchantCreateSchema>
export type WmsMerchantUpdateInput = z.infer<typeof wmsMerchantUpdateSchema>
