// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsItemCategoryRepository } from "../repositories/wms-item-category.repository"
import type {
  WmsItemCategoryCreateDTO,
  WmsItemCategoryPageQuery,
  WmsItemCategoryUpdateDTO,
} from "../types/wms-item-category.types"

export class WmsItemCategoryService {
  static async page(query: WmsItemCategoryPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsItemCategoryRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsItemCategoryRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsItemCategoryCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemCategoryRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItemCategory.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsItemCategoryUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemCategoryRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItemCategory.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsItemCategoryRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsItemCategory.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsItemCategoryService = WmsItemCategoryService
