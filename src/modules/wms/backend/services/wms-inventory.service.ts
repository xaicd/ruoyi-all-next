// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsInventoryRepository } from "../repositories/wms-inventory.repository"
import type {
  WmsInventoryCreateDTO,
  WmsInventoryPageQuery,
  WmsInventoryUpdateDTO,
} from "../types/wms-inventory.types"

export class WmsInventoryService {
  static async page(query: WmsInventoryPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsInventoryRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsInventoryRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsInventoryCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsInventoryRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsInventory.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsInventoryUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsInventoryRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsInventory.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsInventoryRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsInventory.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsInventoryService = WmsInventoryService
