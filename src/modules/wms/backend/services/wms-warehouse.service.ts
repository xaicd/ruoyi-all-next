// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsWarehouseRepository } from "../repositories/wms-warehouse.repository"
import type {
  WmsWarehouseCreateDTO,
  WmsWarehousePageQuery,
  WmsWarehouseUpdateDTO,
} from "../types/wms-warehouse.types"

export class WmsWarehouseService {
  static async page(query: WmsWarehousePageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsWarehouseRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsWarehouseRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsWarehouseCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsWarehouseRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsWarehouse.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsWarehouseUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsWarehouseRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsWarehouse.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsWarehouseRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsWarehouse.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsWarehouseService = WmsWarehouseService
