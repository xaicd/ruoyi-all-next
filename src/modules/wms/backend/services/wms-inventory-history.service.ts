// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsInventoryHistoryRepository } from "../repositories/wms-inventory-history.repository"
import type {
  WmsInventoryHistoryCreateDTO,
  WmsInventoryHistoryPageQuery,
  WmsInventoryHistoryUpdateDTO,
} from "../types/wms-inventory-history.types"

export class WmsInventoryHistoryService {
  static async page(query: WmsInventoryHistoryPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsInventoryHistoryRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsInventoryHistoryRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsInventoryHistoryCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsInventoryHistoryRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsInventoryHistory.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsInventoryHistoryUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsInventoryHistoryRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsInventoryHistory.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsInventoryHistoryRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsInventoryHistory.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsInventoryHistoryService = WmsInventoryHistoryService
