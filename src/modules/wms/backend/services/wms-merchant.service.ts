// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsMerchantRepository } from "../repositories/wms-merchant.repository"
import type {
  WmsMerchantCreateDTO,
  WmsMerchantPageQuery,
  WmsMerchantUpdateDTO,
} from "../types/wms-merchant.types"

export class WmsMerchantService {
  static async page(query: WmsMerchantPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsMerchantRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsMerchantRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsMerchantCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsMerchantRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsMerchant.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsMerchantUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsMerchantRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsMerchant.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsMerchantRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsMerchant.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsMerchantService = WmsMerchantService
