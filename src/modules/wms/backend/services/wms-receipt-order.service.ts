// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsReceiptOrderRepository } from "../repositories/wms-receipt-order.repository"
import type {
  WmsReceiptOrderCreateDTO,
  WmsReceiptOrderPageQuery,
  WmsReceiptOrderUpdateDTO,
} from "../types/wms-receipt-order.types"

export class WmsReceiptOrderService {
  static async page(query: WmsReceiptOrderPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsReceiptOrderRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsReceiptOrderRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsReceiptOrderCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsReceiptOrderRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsReceiptOrder.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsReceiptOrderUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsReceiptOrderRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsReceiptOrder.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsReceiptOrderRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsReceiptOrder.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsReceiptOrderService = WmsReceiptOrderService
