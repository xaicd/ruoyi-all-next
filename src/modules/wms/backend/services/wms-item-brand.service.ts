// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsItemBrandRepository } from "../repositories/wms-item-brand.repository"
import type {
  WmsItemBrandCreateDTO,
  WmsItemBrandPageQuery,
  WmsItemBrandUpdateDTO,
} from "../types/wms-item-brand.types"

export class WmsItemBrandService {
  static async page(query: WmsItemBrandPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsItemBrandRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsItemBrandRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsItemBrandCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemBrandRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItemBrand.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsItemBrandUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemBrandRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItemBrand.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsItemBrandRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsItemBrand.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsItemBrandService = WmsItemBrandService
