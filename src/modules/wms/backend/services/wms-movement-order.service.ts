// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsMovementOrderRepository } from "../repositories/wms-movement-order.repository"
import type {
  WmsMovementOrderCreateDTO,
  WmsMovementOrderPageQuery,
  WmsMovementOrderUpdateDTO,
} from "../types/wms-movement-order.types"

export class WmsMovementOrderService {
  static async page(query: WmsMovementOrderPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsMovementOrderRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsMovementOrderRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsMovementOrderCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsMovementOrderRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsMovementOrder.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsMovementOrderUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsMovementOrderRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsMovementOrder.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsMovementOrderRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsMovementOrder.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsMovementOrderService = WmsMovementOrderService
