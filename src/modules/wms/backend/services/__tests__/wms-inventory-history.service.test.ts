// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsInventoryHistoryService } from "../wms-inventory-history.service"

describe("WmsInventoryHistoryService", () => {
  it("should create and query 库存流水", async () => {
    const created = await WmsInventoryHistoryService.create({
      warehouse_id: "测试库存流水",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsInventoryHistoryService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsInventoryHistoryService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsInventoryHistoryService.update(created.id, {
      id: created.id,
      warehouse_id: "更新库存流水",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsInventoryHistoryService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
