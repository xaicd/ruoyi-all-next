// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsShipmentOrderDetailService } from "../wms-shipment-order-detail.service"

describe("WmsShipmentOrderDetailService", () => {
  it("should create and query 出库明细", async () => {
    const created = await WmsShipmentOrderDetailService.create({
      shipment_order_id: "测试出库明细",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsShipmentOrderDetailService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsShipmentOrderDetailService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsShipmentOrderDetailService.update(created.id, {
      id: created.id,
      shipment_order_id: "更新出库明细",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsShipmentOrderDetailService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
