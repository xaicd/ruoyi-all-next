// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsItemService } from "../wms-item.service"

describe("WmsItemService", () => {
  it("should create and query 物料主数据", async () => {
    const created = await WmsItemService.create({
      item_code: "测试物料主数据",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsItemService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsItemService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsItemService.update(created.id, {
      id: created.id,
      item_code: "更新物料主数据",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsItemService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
