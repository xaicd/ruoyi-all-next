// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsShipmentOrderService } from "../wms-shipment-order.service"

describe("WmsShipmentOrderService", () => {
  it("should create and query 出库单", async () => {
    const created = await WmsShipmentOrderService.create({
      order_no: "测试出库单",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsShipmentOrderService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsShipmentOrderService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsShipmentOrderService.update(created.id, {
      id: created.id,
      order_no: "更新出库单",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsShipmentOrderService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
