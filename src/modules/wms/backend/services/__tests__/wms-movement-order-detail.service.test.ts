// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsMovementOrderDetailService } from "../wms-movement-order-detail.service"

describe("WmsMovementOrderDetailService", () => {
  it("should create and query 移库明细", async () => {
    const created = await WmsMovementOrderDetailService.create({
      movement_order_id: "测试移库明细",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsMovementOrderDetailService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsMovementOrderDetailService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsMovementOrderDetailService.update(created.id, {
      id: created.id,
      movement_order_id: "更新移库明细",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsMovementOrderDetailService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
