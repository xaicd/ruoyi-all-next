// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsMovementOrderService } from "../wms-movement-order.service"

describe("WmsMovementOrderService", () => {
  it("should create and query 移库单", async () => {
    const created = await WmsMovementOrderService.create({
      order_no: "测试移库单",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsMovementOrderService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsMovementOrderService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsMovementOrderService.update(created.id, {
      id: created.id,
      order_no: "更新移库单",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsMovementOrderService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
