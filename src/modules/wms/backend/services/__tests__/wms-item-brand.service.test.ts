// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsItemBrandService } from "../wms-item-brand.service"

describe("WmsItemBrandService", () => {
  it("should create and query 物料品牌", async () => {
    const created = await WmsItemBrandService.create({
      name: "测试物料品牌",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsItemBrandService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsItemBrandService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsItemBrandService.update(created.id, {
      id: created.id,
      name: "更新物料品牌",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsItemBrandService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
