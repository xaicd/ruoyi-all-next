// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsReceiptOrderService } from "../wms-receipt-order.service"

describe("WmsReceiptOrderService", () => {
  it("should create and query 入库单", async () => {
    const created = await WmsReceiptOrderService.create({
      order_no: "测试入库单",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsReceiptOrderService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsReceiptOrderService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsReceiptOrderService.update(created.id, {
      id: created.id,
      order_no: "更新入库单",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsReceiptOrderService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
