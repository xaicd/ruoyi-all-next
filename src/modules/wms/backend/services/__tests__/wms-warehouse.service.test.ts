// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsWarehouseService } from "../wms-warehouse.service"

describe("WmsWarehouseService", () => {
  it("should create and query 智能仓库", async () => {
    const created = await WmsWarehouseService.create({
      code: "测试智能仓库",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsWarehouseService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsWarehouseService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsWarehouseService.update(created.id, {
      id: created.id,
      code: "更新智能仓库",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsWarehouseService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
