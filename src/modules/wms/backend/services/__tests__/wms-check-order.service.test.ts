// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsCheckOrderService } from "../wms-check-order.service"

describe("WmsCheckOrderService", () => {
  it("should create and query 盘点单", async () => {
    const created = await WmsCheckOrderService.create({
      order_no: "测试盘点单",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsCheckOrderService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsCheckOrderService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsCheckOrderService.update(created.id, {
      id: created.id,
      order_no: "更新盘点单",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsCheckOrderService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
