// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsItemSkuService } from "../wms-item-sku.service"

describe("WmsItemSkuService", () => {
  it("should create and query 物料SKU", async () => {
    const created = await WmsItemSkuService.create({
      item_id: "测试物料SKU",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsItemSkuService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsItemSkuService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsItemSkuService.update(created.id, {
      id: created.id,
      item_id: "更新物料SKU",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsItemSkuService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
