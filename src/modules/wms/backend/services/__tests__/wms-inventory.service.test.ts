// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsInventoryService } from "../wms-inventory.service"

describe("WmsInventoryService", () => {
  it("should create and query 实时库存", async () => {
    const created = await WmsInventoryService.create({
      warehouse_id: "测试实时库存",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsInventoryService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsInventoryService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsInventoryService.update(created.id, {
      id: created.id,
      warehouse_id: "更新实时库存",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsInventoryService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
