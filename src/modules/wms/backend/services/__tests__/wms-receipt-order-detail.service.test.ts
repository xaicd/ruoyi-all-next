// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsReceiptOrderDetailService } from "../wms-receipt-order-detail.service"

describe("WmsReceiptOrderDetailService", () => {
  it("should create and query 入库明细", async () => {
    const created = await WmsReceiptOrderDetailService.create({
      receipt_order_id: "测试入库明细",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsReceiptOrderDetailService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsReceiptOrderDetailService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsReceiptOrderDetailService.update(created.id, {
      id: created.id,
      receipt_order_id: "更新入库明细",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsReceiptOrderDetailService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
