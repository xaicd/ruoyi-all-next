// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { WmsItemCategoryService } from "../wms-item-category.service"

describe("WmsItemCategoryService", () => {
  it("should create and query 物料分类", async () => {
    const created = await WmsItemCategoryService.create({
      parent_id: "测试物料分类",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await WmsItemCategoryService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await WmsItemCategoryService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await WmsItemCategoryService.update(created.id, {
      id: created.id,
      parent_id: "更新物料分类",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await WmsItemCategoryService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
