// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsItemSkuRepository } from "../repositories/wms-item-sku.repository"
import type {
  WmsItemSkuCreateDTO,
  WmsItemSkuPageQuery,
  WmsItemSkuUpdateDTO,
} from "../types/wms-item-sku.types"

export class WmsItemSkuService {
  static async page(query: WmsItemSkuPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsItemSkuRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsItemSkuRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsItemSkuCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemSkuRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItemSku.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsItemSkuUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemSkuRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItemSku.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsItemSkuRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsItemSku.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsItemSkuService = WmsItemSkuService
