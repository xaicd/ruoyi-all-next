// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsShipmentOrderDetailRepository } from "../repositories/wms-shipment-order-detail.repository"
import type {
  WmsShipmentOrderDetailCreateDTO,
  WmsShipmentOrderDetailPageQuery,
  WmsShipmentOrderDetailUpdateDTO,
} from "../types/wms-shipment-order-detail.types"

export class WmsShipmentOrderDetailService {
  static async page(query: WmsShipmentOrderDetailPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsShipmentOrderDetailRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsShipmentOrderDetailRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsShipmentOrderDetailCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsShipmentOrderDetailRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsShipmentOrderDetail.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsShipmentOrderDetailUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsShipmentOrderDetailRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsShipmentOrderDetail.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsShipmentOrderDetailRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsShipmentOrderDetail.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsShipmentOrderDetailService = WmsShipmentOrderDetailService
