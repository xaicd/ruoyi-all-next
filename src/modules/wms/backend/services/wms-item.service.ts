// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsItemRepository } from "../repositories/wms-item.repository"
import type {
  WmsItemCreateDTO,
  WmsItemPageQuery,
  WmsItemUpdateDTO,
} from "../types/wms-item.types"

export class WmsItemService {
  static async page(query: WmsItemPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsItemRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsItemRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsItemCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItem.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsItemUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsItemRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsItem.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsItemRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsItem.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsItemService = WmsItemService
