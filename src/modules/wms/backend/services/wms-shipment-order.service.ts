// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsShipmentOrderRepository } from "../repositories/wms-shipment-order.repository"
import type {
  WmsShipmentOrderCreateDTO,
  WmsShipmentOrderPageQuery,
  WmsShipmentOrderUpdateDTO,
} from "../types/wms-shipment-order.types"

export class WmsShipmentOrderService {
  static async page(query: WmsShipmentOrderPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsShipmentOrderRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsShipmentOrderRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsShipmentOrderCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsShipmentOrderRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsShipmentOrder.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsShipmentOrderUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsShipmentOrderRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsShipmentOrder.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsShipmentOrderRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsShipmentOrder.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsShipmentOrderService = WmsShipmentOrderService
