// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsReceiptOrderDetailRepository } from "../repositories/wms-receipt-order-detail.repository"
import type {
  WmsReceiptOrderDetailCreateDTO,
  WmsReceiptOrderDetailPageQuery,
  WmsReceiptOrderDetailUpdateDTO,
} from "../types/wms-receipt-order-detail.types"

export class WmsReceiptOrderDetailService {
  static async page(query: WmsReceiptOrderDetailPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsReceiptOrderDetailRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsReceiptOrderDetailRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsReceiptOrderDetailCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsReceiptOrderDetailRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsReceiptOrderDetail.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsReceiptOrderDetailUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsReceiptOrderDetailRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsReceiptOrderDetail.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsReceiptOrderDetailRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsReceiptOrderDetail.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsReceiptOrderDetailService = WmsReceiptOrderDetailService
