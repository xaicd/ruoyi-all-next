// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsCheckOrderRepository } from "../repositories/wms-check-order.repository"
import type {
  WmsCheckOrderCreateDTO,
  WmsCheckOrderPageQuery,
  WmsCheckOrderUpdateDTO,
} from "../types/wms-check-order.types"

export class WmsCheckOrderService {
  static async page(query: WmsCheckOrderPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsCheckOrderRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsCheckOrderRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsCheckOrderCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsCheckOrderRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsCheckOrder.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsCheckOrderUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsCheckOrderRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsCheckOrder.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsCheckOrderRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsCheckOrder.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsCheckOrderService = WmsCheckOrderService
