// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { WmsMovementOrderDetailRepository } from "../repositories/wms-movement-order-detail.repository"
import type {
  WmsMovementOrderDetailCreateDTO,
  WmsMovementOrderDetailPageQuery,
  WmsMovementOrderDetailUpdateDTO,
} from "../types/wms-movement-order-detail.types"

export class WmsMovementOrderDetailService {
  static async page(query: WmsMovementOrderDetailPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return WmsMovementOrderDetailRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return WmsMovementOrderDetailRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: WmsMovementOrderDetailCreateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsMovementOrderDetailRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsMovementOrderDetail.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: WmsMovementOrderDetailUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await WmsMovementOrderDetailRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("wms.wmsMovementOrderDetail.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await WmsMovementOrderDetailRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("wms.wmsMovementOrderDetail.deleted", { id, actorId })
    }
    return ok
  }
}

export const wmsMovementOrderDetailService = WmsMovementOrderDetailService
