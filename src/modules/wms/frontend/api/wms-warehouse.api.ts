// Auto-generated by Codegen Engine (Frontend API Client)
import { request } from "@/modules/shared/frontend/lib/request"
import type {
  WmsWarehouseCreateDTO,
  WmsWarehousePageQuery,
  WmsWarehouseUpdateDTO,
  WmsWarehouseVO,
} from "@/modules/wms/backend/types/wms-warehouse.types"

const BASE_URL = "/api/v1/admin/wms/wms-warehouse"

export const WmsWarehouseApi = {
  async page(params?: WmsWarehousePageQuery) {
    return request.get<{ items: WmsWarehouseVO[]; total: number }>(BASE_URL, { params })
  },

  async get(id: string) {
    return request.get<WmsWarehouseVO>(`${BASE_URL}/${id}`)
  },

  async create(data: WmsWarehouseCreateDTO) {
    return request.post<WmsWarehouseVO>(BASE_URL, data)
  },

  async update(data: WmsWarehouseUpdateDTO) {
    return request.put<WmsWarehouseVO>(BASE_URL, data)
  },

  async delete(id: string) {
    return request.delete<void>(`${BASE_URL}?id=${id}`)
  },

  async export(params?: WmsWarehousePageQuery) {
    return request.post<WmsWarehouseVO[]>(`${BASE_URL}/export`, params)
  },

  async getImportTemplate() {
    return request.get<{ columns: string[]; sampleRows: Record<string, unknown>[] }>(`${BASE_URL}/import-template`)
  },

  async import(file: File) {
    const formData = new FormData()
    formData.append("file", file)
    return request.post<{ importedCount: number }>(`${BASE_URL}/import`, formData)
  },
}
