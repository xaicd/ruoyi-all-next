// Auto-generated by Codegen Engine (Frontend API Client)
import { request } from "@/modules/shared/frontend/lib/request"
import type {
  WmsItemSkuCreateDTO,
  WmsItemSkuPageQuery,
  WmsItemSkuUpdateDTO,
  WmsItemSkuVO,
} from "@/modules/wms/backend/types/wms-item-sku.types"

const BASE_URL = "/api/v1/admin/wms/wms-item-sku"

export const WmsItemSkuApi = {
  async page(params?: WmsItemSkuPageQuery) {
    return request.get<{ items: WmsItemSkuVO[]; total: number }>(BASE_URL, { params })
  },

  async get(id: string) {
    return request.get<WmsItemSkuVO>(`${BASE_URL}/${id}`)
  },

  async create(data: WmsItemSkuCreateDTO) {
    return request.post<WmsItemSkuVO>(BASE_URL, data)
  },

  async update(data: WmsItemSkuUpdateDTO) {
    return request.put<WmsItemSkuVO>(BASE_URL, data)
  },

  async delete(id: string) {
    return request.delete<void>(`${BASE_URL}?id=${id}`)
  },

  async export(params?: WmsItemSkuPageQuery) {
    return request.post<WmsItemSkuVO[]>(`${BASE_URL}/export`, params)
  },

  async getImportTemplate() {
    return request.get<{ columns: string[]; sampleRows: Record<string, unknown>[] }>(`${BASE_URL}/import-template`)
  },

  async import(file: File) {
    const formData = new FormData()
    formData.append("file", file)
    return request.post<{ importedCount: number }>(`${BASE_URL}/import`, formData)
  },
}
