// Auto-generated by Codegen Engine (Frontend API Client)
import { request } from "@/modules/shared/frontend/lib/request"
import type {
  WmsShipmentOrderCreateDTO,
  WmsShipmentOrderPageQuery,
  WmsShipmentOrderUpdateDTO,
  WmsShipmentOrderVO,
} from "@/modules/wms/backend/types/wms-shipment-order.types"

const BASE_URL = "/api/v1/admin/wms/wms-shipment-order"

export const WmsShipmentOrderApi = {
  async page(params?: WmsShipmentOrderPageQuery) {
    return request.get<{ items: WmsShipmentOrderVO[]; total: number }>(BASE_URL, { params })
  },

  async get(id: string) {
    return request.get<WmsShipmentOrderVO>(`${BASE_URL}/${id}`)
  },

  async create(data: WmsShipmentOrderCreateDTO) {
    return request.post<WmsShipmentOrderVO>(BASE_URL, data)
  },

  async update(data: WmsShipmentOrderUpdateDTO) {
    return request.put<WmsShipmentOrderVO>(BASE_URL, data)
  },

  async delete(id: string) {
    return request.delete<void>(`${BASE_URL}?id=${id}`)
  },

  async export(params?: WmsShipmentOrderPageQuery) {
    return request.post<WmsShipmentOrderVO[]>(`${BASE_URL}/export`, params)
  },

  async getImportTemplate() {
    return request.get<{ columns: string[]; sampleRows: Record<string, unknown>[] }>(`${BASE_URL}/import-template`)
  },

  async import(file: File) {
    const formData = new FormData()
    formData.append("file", file)
    return request.post<{ importedCount: number }>(`${BASE_URL}/import`, formData)
  },
}
