// Auto-generated by Codegen Engine (Frontend API Client)
import { request } from "@/modules/shared/frontend/lib/request"
import type {
  WmsMerchantCreateDTO,
  WmsMerchantPageQuery,
  WmsMerchantUpdateDTO,
  WmsMerchantVO,
} from "@/modules/wms/backend/types/wms-merchant.types"

const BASE_URL = "/api/v1/admin/wms/wms-merchant"

export const WmsMerchantApi = {
  async page(params?: WmsMerchantPageQuery) {
    return request.get<{ items: WmsMerchantVO[]; total: number }>(BASE_URL, { params })
  },

  async get(id: string) {
    return request.get<WmsMerchantVO>(`${BASE_URL}/${id}`)
  },

  async create(data: WmsMerchantCreateDTO) {
    return request.post<WmsMerchantVO>(BASE_URL, data)
  },

  async update(data: WmsMerchantUpdateDTO) {
    return request.put<WmsMerchantVO>(BASE_URL, data)
  },

  async delete(id: string) {
    return request.delete<void>(`${BASE_URL}?id=${id}`)
  },

  async export(params?: WmsMerchantPageQuery) {
    return request.post<WmsMerchantVO[]>(`${BASE_URL}/export`, params)
  },

  async getImportTemplate() {
    return request.get<{ columns: string[]; sampleRows: Record<string, unknown>[] }>(`${BASE_URL}/import-template`)
  },

  async import(file: File) {
    const formData = new FormData()
    formData.append("file", file)
    return request.post<{ importedCount: number }>(`${BASE_URL}/import`, formData)
  },
}
