// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { AigwPartnerService } from "../aigw-partner.service"

describe("AigwPartnerService", () => {
  it("should create and query 渠道代理商", async () => {
    const created = await AigwPartnerService.create({
      partner_code: "测试渠道代理商",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await AigwPartnerService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await AigwPartnerService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await AigwPartnerService.update(created.id, {
      id: created.id,
      partner_code: "更新渠道代理商",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await AigwPartnerService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
