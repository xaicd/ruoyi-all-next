// Auto-generated by Codegen Engine (Service Unit Test)
import { describe, it, expect } from "vitest"
import { AigwPartnerLeadService } from "../aigw-partner-lead.service"

describe("AigwPartnerLeadService", () => {
  it("should create and query 商机报备与锁定", async () => {
    const created = await AigwPartnerLeadService.create({
      lead_no: "测试商机报备与锁定",
    } as any)
    expect(created).toBeDefined()
    expect(created.id).toBeDefined()

    const list = await AigwPartnerLeadService.page({ page: 1, pageSize: 10 })
    expect(list.items.length).toBeGreaterThan(0)

    const fetched = await AigwPartnerLeadService.get(created.id)
    expect(fetched).toBeDefined()
    expect(fetched?.id).toBe(created.id)

    const updated = await AigwPartnerLeadService.update(created.id, {
      id: created.id,
      lead_no: "更新商机报备与锁定",
    } as any)
    expect(updated).toBeDefined()

    const deleted = await AigwPartnerLeadService.delete(created.id)
    expect(deleted).toBe(true)
  })
})
