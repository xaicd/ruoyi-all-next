// Auto-generated by Codegen Engine (RPC Facade / Dual-Mode Binding)
import { serviceBus } from "@/modules/shared/backend/lib/service-bus"
import { AigwPartnerService } from "./aigw-partner.service"
import type {
  AigwPartnerCreateDTO,
  AigwPartnerPageQuery,
  AigwPartnerUpdateDTO,
} from "../types/aigw-partner.types"

export class AigwPartnerRpc {
  static async register() {
    serviceBus.registerHandler("ruoyi.cmd.aigw.aigwPartner.page", async (req) => {
      const query = req.data as AigwPartnerPageQuery
      return AigwPartnerService.page(query, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartner.get", async (req) => {
      const { id } = req.data as { id: string }
      return AigwPartnerService.get(id, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartner.create", async (req) => {
      const data = req.data as AigwPartnerCreateDTO
      return AigwPartnerService.create(data, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartner.update", async (req) => {
      const { id, ...rest } = req.data as AigwPartnerUpdateDTO
      return AigwPartnerService.update(id, rest as any, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartner.delete", async (req) => {
      const { id } = req.data as { id: string }
      return AigwPartnerService.delete(id, req.actorId, req.tenantId)
    })
  }
}

export const aigwPartnerRpc = AigwPartnerRpc
