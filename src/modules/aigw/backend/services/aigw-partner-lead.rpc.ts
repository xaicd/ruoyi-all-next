// Auto-generated by Codegen Engine (RPC Facade / Dual-Mode Binding)
import { serviceBus } from "@/modules/shared/backend/lib/service-bus"
import { AigwPartnerLeadService } from "./aigw-partner-lead.service"
import type {
  AigwPartnerLeadCreateDTO,
  AigwPartnerLeadPageQuery,
  AigwPartnerLeadUpdateDTO,
} from "../types/aigw-partner-lead.types"

export class AigwPartnerLeadRpc {
  static async register() {
    serviceBus.registerHandler("ruoyi.cmd.aigw.aigwPartnerLead.page", async (req) => {
      const query = req.data as AigwPartnerLeadPageQuery
      return AigwPartnerLeadService.page(query, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartnerLead.get", async (req) => {
      const { id } = req.data as { id: string }
      return AigwPartnerLeadService.get(id, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartnerLead.create", async (req) => {
      const data = req.data as AigwPartnerLeadCreateDTO
      return AigwPartnerLeadService.create(data, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartnerLead.update", async (req) => {
      const { id, ...rest } = req.data as AigwPartnerLeadUpdateDTO
      return AigwPartnerLeadService.update(id, rest as any, req.actorId, req.tenantId)
    })

    bus.registerHandler("ruoyi.cmd.aigw.aigwPartnerLead.delete", async (req) => {
      const { id } = req.data as { id: string }
      return AigwPartnerLeadService.delete(id, req.actorId, req.tenantId)
    })
  }
}

export const aigwPartnerLeadRpc = AigwPartnerLeadRpc
