// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { AigwPartnerRepository } from "../repositories/aigw-partner.repository"
import type {
  AigwPartnerCreateDTO,
  AigwPartnerPageQuery,
  AigwPartnerUpdateDTO,
} from "../types/aigw-partner.types"

export class AigwPartnerService {
  static async page(query: AigwPartnerPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return AigwPartnerRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return AigwPartnerRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: AigwPartnerCreateDTO, actorId?: string, tenantId?: string) {
    const row = await AigwPartnerRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("aigw.aigwPartner.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: AigwPartnerUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await AigwPartnerRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("aigw.aigwPartner.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await AigwPartnerRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("aigw.aigwPartner.deleted", { id, actorId })
    }
    return ok
  }
}

export const aigwPartnerService = AigwPartnerService
