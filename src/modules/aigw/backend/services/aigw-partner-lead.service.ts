// Auto-generated by Codegen Engine
import { domainLog } from "@/modules/shared/backend/lib/domain-log"
import { AigwPartnerLeadRepository } from "../repositories/aigw-partner-lead.repository"
import type {
  AigwPartnerLeadCreateDTO,
  AigwPartnerLeadPageQuery,
  AigwPartnerLeadUpdateDTO,
} from "../types/aigw-partner-lead.types"

export class AigwPartnerLeadService {
  static async page(query: AigwPartnerLeadPageQuery, actorId?: string, tenantId?: string) {
    const page = query.page ?? 1
    const pageSize = query.pageSize ?? 20
    const filter = { ...query }
    delete (filter as Record<string, unknown>).page
    delete (filter as Record<string, unknown>).pageSize

    return AigwPartnerLeadRepository.page(
      { page, pageSize, ...filter },
      { actorId, tenantId }
    )
  }

  static async get(id: string, actorId?: string, tenantId?: string) {
    return AigwPartnerLeadRepository.findById(id, { actorId, tenantId })
  }

  static async create(data: AigwPartnerLeadCreateDTO, actorId?: string, tenantId?: string) {
    const row = await AigwPartnerLeadRepository.create(data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("aigw.aigwPartnerLead.created", { id: row.id, actorId })
    return row
  }

  static async update(id: string, data: AigwPartnerLeadUpdateDTO, actorId?: string, tenantId?: string) {
    const row = await AigwPartnerLeadRepository.update(id, data as unknown as Record<string, unknown>, { actorId, tenantId })
    domainLog.event("aigw.aigwPartnerLead.updated", { id, actorId })
    return row
  }

  static async delete(id: string, actorId?: string, tenantId?: string) {
    const ok = await AigwPartnerLeadRepository.delete(id, { actorId, tenantId })
    if (ok) {
      domainLog.event("aigw.aigwPartnerLead.deleted", { id, actorId })
    }
    return ok
  }
}

export const aigwPartnerLeadService = AigwPartnerLeadService
