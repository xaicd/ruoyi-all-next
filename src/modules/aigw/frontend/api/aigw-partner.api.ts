// Auto-generated by Codegen Engine (Frontend API Client)
import { request } from "@/modules/shared/frontend/lib/request"
import type {
  AigwPartnerCreateDTO,
  AigwPartnerPageQuery,
  AigwPartnerUpdateDTO,
  AigwPartnerVO,
} from "@/modules/aigw/backend/types/aigw-partner.types"

const BASE_URL = "/api/v1/admin/aigw/aigw-partner"

export const AigwPartnerApi = {
  async page(params?: AigwPartnerPageQuery) {
    return request.get<{ items: AigwPartnerVO[]; total: number }>(BASE_URL, { params })
  },

  async get(id: string) {
    return request.get<AigwPartnerVO>(`${BASE_URL}/${id}`)
  },

  async create(data: AigwPartnerCreateDTO) {
    return request.post<AigwPartnerVO>(BASE_URL, data)
  },

  async update(data: AigwPartnerUpdateDTO) {
    return request.put<AigwPartnerVO>(BASE_URL, data)
  },

  async delete(id: string) {
    return request.delete<void>(`${BASE_URL}?id=${id}`)
  },

  async export(params?: AigwPartnerPageQuery) {
    return request.post<AigwPartnerVO[]>(`${BASE_URL}/export`, params)
  },

  async getImportTemplate() {
    return request.get<{ columns: string[]; sampleRows: Record<string, unknown>[] }>(`${BASE_URL}/import-template`)
  },

  async import(file: File) {
    const formData = new FormData()
    formData.append("file", file)
    return request.post<{ importedCount: number }>(`${BASE_URL}/import`, formData)
  },
}
