# Generated from src/modules/shared/backend/constants/domain-catalog.json
# Do not hand-edit pack/upstream fields. Run: npm run domain:manifests
domain: ai
owner: ai
kind: business
stage: B
contractVersion: v1
implementation: local-ts
publicPrefix:
  - /api/v1/admin/ai
  - /api/v1/open/ai
upstream:
  env: RUOYI_DOMAIN_AI_UPSTREAM
  protocol: http
  defaultPort: 3223
auth:
  audience: admin
  tenantPolicy: required
resilience:
  timeoutMs: 15000
  retry:
    maxAttempts: 1
    safeMethodsOnly: true
  idempotencyRequired: true
rollout:
  mode: local
messaging:
  transport: in-process
  queueGroup: ruoyi.ai
  commandSubject: ruoyi.cmd.ai.>
  eventSubject: ruoyi.evt.ai.>
invoke:
  colocated: sdk
  remote: rpc
  protocol: nats-rr
  serialization: json
  facade: required
pack:
  enabled: true
  kind: api-only
  independentDatabase: false
  dependsOnModules:
    - shared
    - ai
