# Online management pages

The Online Definition workbench and Puck renderer are added after the versioned metadata persistence and published-release resolver exist. The legacy `infra/page-builder` remains a migration source and is not reused as the Online runtime.
