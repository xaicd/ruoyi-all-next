# Online persistence adapter

This directory intentionally has no runtime repository in phase 1. The next phase adds tenant-scoped Prisma metadata models and a Kysely repository; production must never fall back to process-local Online definition storage.
