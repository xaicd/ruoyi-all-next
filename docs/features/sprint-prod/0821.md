# 2026-08-21 需求记录

## 1. 那你 为啥开始 搞别的，system,infra 得 搞完搞全

**用户原始输入：**

那你 为啥开始 搞别的，system,infra 得 搞完搞全

**落地说明：**

先前把 D18–D20 的 P0 核心当收口、把 D17 业务域排到前面，偏离用户要求。本轮只做 system / infra 剩余 HTTP 进 Facade（D11 平台域范围），不碰 mall/crm/bpm。

- system：字典数据、租户套餐、权限分配、登录/操作日志、在线用户、通知、邮件、短信、OAuth2、社交、地区 进 Facade + ACTION_SCHEMAS
- infra：codegen 表 CRUD、数据源测试、日志导出、审计留存 进 Facade；preview/generate HTTP 解析走已有 schema
- 登录审计/ZIP 打包仍在 BFF 薄层；解析与业务均走 Facade
- mail-template / sms-template / social-client 已实现 setting 存储 CRUD，不再 501

## 2. 继续只做这两域

**用户原始输入：**

继续只做这两域

**落地说明：**

继续收口 system / infra 剩余：user-profile、过期 OAuth2、codegen 候选表/导入/下载、501 空壳，以及登录/验证码/侧栏中能进 Facade 的部分。不碰 mall/crm/bpm。

本轮已落地：登录/验证码/侧栏/资料/OAuth2 旧入口/邮件短信模板/社交客户端/codegen 候选表导入与 ZIP 生成进 Facade。HTTP 仍负责登录审计与 ZIP 打包。

## 3. system,infra 其实不用整体 对外的， 只有 数据字典，登录用户信息查询

**用户原始输入：**

system,infra 其实不用整体 对外的， 只有 数据字典，登录用户信息查询

**落地说明：**

system / infra 不对业务域整体开放 RPC。对外只保留：

- 数据字典：`getDictDataByType`
- 登录用户信息：`getPermissionInfoByUser`

其余后台 CRUD 仍走本域 HTTP + 本地 Service；`systemFacade` / `infraFacade` 全量方法仅给 BFF 同域双模。shared 鉴权走平台面 `resolveTenantEntitlement`；Online 预览/下载与报表数据源走 infra 平台面，不算对外业务 API。

## 4. Execute the selected diff-tab commit-and-push action.

**用户原始输入：**

Execute the selected diff-tab commit-and-push action.
- Current branch: main.
- Base branch: master.
- Commit only the already-staged files listed below.
- The staged file list below is authoritative; do not re-check it.
- Do not stage additional files; commit only the staged portions.
- Write a concise commit message.
- Push after creating the commit.

## 5. 继续 剩余待办

**用户原始输入：**

继续 剩余待办

**落地说明：**

对照 CURRENT-PROGRESS 剩余 TODO（D12/D13/D17）与未提交的 system/infra 收口。先把未落地缺口补齐，再按清单推进；不碰 D12/D13。

本轮落地 D17：mall/crm/bpm 以及 member/erp/report/mp/wms/mes/ai/iot/im 已有门面方法对应的列表（及 mall 发券、crm 跟进）HTTP 改走各域 `ACTION_SCHEMAS` + `parseActionQuery`/`parseActionBody`。不碰 D12/D13。

## 6. Execute the selected diff-tab commit-and-push action.

**用户原始输入：**

Execute the selected diff-tab commit-and-push action.
- Current branch: main.
- Base branch: master.
- Stage the changes you worked on.
- If there are unrelated files or changes, exclude them.
- Write a concise commit message.
- Push after creating the commit.
- Do not include unrelated files in the commit.

## 7. 在线开发，数据库驱动生成代码是否都与新架构微服务模块化一致了

**用户原始输入：**

在线开发，  数据库 驱动生成代码  是否都 与 新 架构 微服务 模块化 一致了

## 8. 开始吧

**用户原始输入：**

开始吧

**落地说明：**

收口在线开发与表驱动生成与新架构的剩余缺口：生成 Route 走 `ACTION_SCHEMAS` + `parseActionQuery`/`parseActionBody`；生成契约登记片段；online 测试不再直接 import `CodegenEngineService`。不碰 D12/D13，不把 MOCK_DATA 改成真库。

## 9. Execute the selected diff-tab commit-and-push action.

**用户原始输入：**

Execute the selected diff-tab commit-and-push action.
- Current branch: main.
- Base branch: master.
- Stage the changes you worked on.
- If there are unrelated files or changes, exclude them.
- Write a concise commit message.
- Push after creating the commit.
- Do not include unrelated files in the commit.

