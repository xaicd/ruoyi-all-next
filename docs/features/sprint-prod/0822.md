# 2026-08-22 需求记录

## 1. grpc 接口有哪些，要不要动态注册 / 发现 / 调用

**用户原始输入：**

grpc 接口 有哪些， 怎么需要 支持 动态 注册 接口，动态发现 接口，动态 调用接口吗，

## 2. 运行时注册 vs Dubbo/ZK，K8s gRPC 是否把接口动态注册到 etcd

**用户原始输入：**

运行时  注册 能力 ，比如 dubbo, zookeeper 这些 不够优秀吗， k8s底层的  grpc 调用是否 动态注册 接口到 etcd 了

## 3. 咱们还有什么能做

**用户原始输入：**

咱们 还有什么能做

## 4. system 剩余 MOCK 或 infra 文件/调度实装

**用户原始输入：**

```chat_selection
agent_id: ff4868b9-c435-49ce-926e-9d8888463746
selected_text:
system 剩余 MOCK 或 infra 文件/调度实装
```

## 5. 功能测试要对齐 JeecgBoot 在线表单功能测试

**用户原始输入：**

功能测试 那个 功能 还是要求  和 jeecgboot 在线 表单 功能测试 一致

## 6. 开始吧

**用户原始输入：**

开始吧


